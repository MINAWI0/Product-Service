spring:

  application:
    name: Product
  datasource:
    url: jdbc:mysql://localhost:3306/Product
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8081
